#include<bits/stdc++.h>
using namespace std;
int main()
{
	string s;
	cin>>s;
	int a[s.length()];
	for(int i=0;i<s.length();i++)
	{
		a[i]=s[i];
		k=k+(a[i]-48)*pow(10,i);
	}
	cout<<a[]
}
